<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/base.css">
  <link rel="stylesheet" href="../moviedetail.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <title>Movie Details</title>
</head>

<body>
  <!-- Header -->
  <%- include("../partials/header.ejs") %>

    <div class="container">

      <img src="<%= movie.image %>" alt="<%= movie.title %>" class="movie__detail__img" />



      <div class="movie__detail__content">
        <h3>
          <%= movie.title %>
        </h3>


        <p>
          <%= movie.description %>
        </p>

        <p>
          Rating: <span class="movie__rating">
            <%= movie.rating %>
          </span>
        </p>

        <p>
          Duration: <span class="movie__duration">
            <%= movie.duration %>
          </span>
        </p>



        <div class="movie__detail__btn">



          <a href="/"><button>Back to home</button>
          </a>

          <% if (user) { %>
            <a href="/movie/edit/<%= movie._id %>">
              <button>Edit Movie</button>
            </a>
            <form action="/movies/<%= movie._id %>?_method=DELETE" method="POST">
              <button class="delete__btn">Delete</button>
            </form>
            <% } %>

        </div>


      </div>




    </div>



</body>

</html>